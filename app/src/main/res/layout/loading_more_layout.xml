<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/common_load_more_footer_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="@dimen/space_8"
    android:paddingBottom="@dimen/space_8">

    <com.github.library.progress.CircularProgressBar
        android:id="@+id/loading_more_pb"
        android:layout_width="@dimen/loading_more_pb_size"
        android:layout_height="@dimen/loading_more_pb_size"
        android:indeterminate="true"
        android:indeterminateOnly="false"
        android:layout_centerInParent="true"
        app:cpb_colors="@array/gplus_colors"/>

    <TextView
        android:id="@+id/loading_more_msg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_centerInParent="true"
        android:visibility="gone"
        android:text="@string/loading"
        android:textColor="@color/background_floating_material_dark"
        android:textSize="@dimen/text_size_15"/>
</RelativeLayout>